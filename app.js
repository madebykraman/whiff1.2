const stages=[['AJAAB','Curiosity','The unfamiliar room. The door you almost did not open.'],['UNS','Attraction','You notice them before you know why.'],['KHUMAAR','Intoxication','The hour when restraint becomes optional.'],['ASRAAR','Mystery','Not everything asks to be understood.'],['TALAB','Desire','Wanting is its own kind of weather.'],['QURBAT','Closeness','Some closeness is almost silent.'],['FIRAQ','Separation','Some distances leave a scent behind.'],['YAAD','Memory','Memory never arrives in order.'],['ISHQ','Love','At some point the question disappears.']];
const getCart=()=>JSON.parse(localStorage.getItem('whiff-cart')||'[]'); const setCart=c=>{localStorage.setItem('whiff-cart',JSON.stringify(c));document.querySelectorAll('[data-cart-count]').forEach(x=>x.textContent=c.length)}; setCart(getCart());
document.querySelectorAll('[data-add]').forEach(b=>b.addEventListener('click',()=>{const c=getCart();c.push(b.dataset.add);setCart(c);b.textContent='Added';setTimeout(()=>b.textContent='Add to cart',900)}));
const result=document.getElementById('result');document.querySelectorAll('.choice').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.choice').forEach(x=>x.classList.remove('active'));b.classList.add('active');const s=stages[+b.dataset.stage-1];if(result)result.innerHTML='Your stage: <b>'+s[0]+'</b>.<br>'+s[2]}));
const cart=document.getElementById('cart');if(cart){const c=getCart();cart.innerHTML=c.length?c.map((x,i)=>'<div class="cart-row"><span>0'+(i+1)+'</span><b>'+x+'</b><strong>₹299</strong></div>').join('')+'<div class="total"><span>Total</span><strong>₹'+c.length*299+'</strong></div><a class="cta" href="checkout.html">Checkout</a>':'<p class="serif">Your stages are waiting.</p>'}
